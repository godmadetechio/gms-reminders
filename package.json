{
  "name": "gms-reminders",
  "version": "1.0.0",
  "description": "GMS Slack call reminders — triggered by Railway cron",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
